export interface loginPageProps {
    email: string;
    password: string;
}

export interface registrationProps{
    email: string;
    password: string;
    confirmPassword: string;
    isAdmin: boolean;
}
